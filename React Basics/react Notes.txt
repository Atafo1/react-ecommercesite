NOTE: THESE ARE BASCICALLY FILLES UNDER THE CHATBOT-PROJECT FOLDE HERE!!!!
node modules folde is where npm install all packages
src folder containe the code for our website
react code are .jsx
the assets folder conataines images needed for our site .Basically where wewill store them
public folder conatains files that can be accessed by the vite directly
git this helps us track changes in our code 
eslint.config.js is used eslint to highlight problems in out javascript code
the / conatains the html codes for the site
package.json has a list of packages that we need to install
package.lock.json it saves the version numbers of the file we install
README.md provides info on the project
vite.config.js configures vite.Vite is a tool used to set up this project
and also builds the site
npm install is to install all the packages in packages.json in that folder
git helps track changes in our code
A repository is just a folder where git is tracking changes
.gitignore tells git the files to ignore or to not track changes
routing helps us create multiple pages in react
install react-router to access the routing feature
ctrl + f to find and replace
ctrl +shift + p to use format document
 fetch gest the data from the backend which in that link
    fetch('http://localhost:3000/api/products')
  
    ,then helps us get the data because fetch there is more like an
    asynchronous code it does not finish right away
    response is the output given from the backend
    .then((response)=>{
        //.json gives us the data attached to the response
           response.json().then((data) =>{
               console.log(data);
           })
    })
    AXIOS IS CLEANER WAY TO MAKE REQUESTS TO THE BACKEND
    LIKE DOWN BELOW IT GAVE US DIRECT ACCESS TO THE DATA IT CONVERTS
    IT TO JSON ALREADY